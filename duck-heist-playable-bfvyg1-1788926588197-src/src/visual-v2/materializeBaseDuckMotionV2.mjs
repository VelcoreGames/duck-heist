import {readFileSync,writeFileSync} from 'node:fs';
import path from 'node:path';

const MARKER='DUCK_BASE_MOTION_V2';

export function applyDuckBaseMotionV2(gameDir){
  const file=path.join(gameDir,'game','sprites.ts');
  let src=readFileSync(file,'utf8');
  if(src.includes(MARKER))return;

  const start=src.indexOf('function drawBaseDuckV2(');
  const end=src.indexOf('\n\nexport function drawDuckSkin(',start);
  if(start<0||end<0)throw new Error('Base duck motion V2: drawBaseDuckV2 block not found');

  const before=src.slice(0,start);
  const after=src.slice(end);
  const replacement=`// ${MARKER}\nfunction drawDuckAngryFaceV2(ctx:Ctx,x:number,y:number,dir:DuckDir='down'){\n  const bx=Math.floor(x),by=Math.floor(y);\n  ctx.save();\n  ctx.imageSmoothingEnabled=false;\n  const ink='#1a120e';\n  const glare='#5d2d0c';\n  if(dir==='left'){\n    ctx.fillStyle=ink;\n    ctx.fillRect(bx+1,by+2,4,1);ctx.fillRect(bx+2,by+3,3,1);\n    ctx.fillRect(bx+6,by+3,3,1);ctx.fillRect(bx+6,by+2,4,1);\n    ctx.fillRect(bx+2,by+5,2,1);ctx.fillRect(bx+7,by+5,2,1);\n  }else{\n    ctx.fillStyle=ink;\n    ctx.fillRect(bx+7,by+3,4,1);ctx.fillRect(bx+8,by+2,3,1);\n    ctx.fillRect(bx+12,by+2,4,1);ctx.fillRect(bx+13,by+3,3,1);\n    ctx.fillRect(bx+8,by+5,2,1);ctx.fillRect(bx+13,by+5,2,1);\n  }\n  ctx.fillStyle=glare;\n  if(dir==='left')ctx.fillRect(bx+3,by+9,4,1);\n  else ctx.fillRect(bx+10,by+9,4,1);\n  ctx.restore();\n}\n\nfunction drawBaseDuckFootV2(ctx:Ctx,x:number,y:number,lifted=false,flip=false){\n  const bx=Math.round(x),by=Math.round(y);\n  const outline='#32180b';\n  const dark='#a95b16';\n  const orange='#f39a2b';\n  const light='#ffc061';\n  ctx.save();\n  ctx.imageSmoothingEnabled=false;\n  if(flip){ctx.translate(bx+6,0);ctx.scale(-1,1);}\n  const ox=flip?0:bx;\n  const fy=by+(lifted?-1:0);\n  ctx.fillStyle=outline;\n  ctx.fillRect(ox+1,fy,4,1);\n  ctx.fillRect(ox,fy+1,6,2);\n  ctx.fillRect(ox+1,fy+3,5,1);\n  ctx.fillStyle=orange;\n  ctx.fillRect(ox+1,fy+1,4,2);\n  ctx.fillRect(ox+4,fy+2,1,1);\n  ctx.fillStyle=light;\n  ctx.fillRect(ox+2,fy+1,2,1);\n  ctx.fillStyle=dark;\n  ctx.fillRect(ox+1,fy+3,4,1);\n  ctx.restore();\n}\n\nfunction drawBaseDuckV2(ctx:Ctx,x:number,y:number,frame:number,dir:DuckDir='down',moving=false,hurt=false,dashing=false,shooting=false,dead=false){\n  const img=getV2Image(V2_BASE_DUCK.src);\n  if(!img||!img.complete||img.naturalWidth<V2_BASE_DUCK.frameW)return true;\n\n  const bx=Math.floor(x),by=Math.floor(y);\n  const step=moving?Math.floor(frame/5)%4:0;\n  const idleStep=Math.floor(frame/18)%4;\n  const alpha=hurt&&Math.floor(frame*.5)%2===0?.48:dashing?.82:1;\n  const flipX=dir==='left';\n  const centerX=bx+8;\n\n  // The body is intentionally independent from the walking cycle. During movement\n  // it stays fixed; only the feet animate. Idle keeps a one-pixel breathing motion.\n  const bodyBob=moving?0:[0,-1,0,0][idleStep];\n  const bodyW=28,bodyH=23;\n  const bodyX=Math.round(centerX-bodyW/2);\n  const bodyY=Math.round(by+14-bodyH+bodyBob);\n\n  ctx.save();\n  ctx.imageSmoothingEnabled=false;\n  ctx.globalAlpha=alpha;\n\n  if(dead){\n    ctx.translate(centerX,by+10);ctx.rotate(Math.PI/2);ctx.translate(-centerX,-(by+10));\n  }\n\n  if(!dead){\n    ctx.save();ctx.globalAlpha=.16;ctx.fillStyle='#080b10';\n    ctx.beginPath();ctx.ellipse(centerX,by+19,moving?5.5:5,1.8,0,0,Math.PI*2);ctx.fill();ctx.restore();\n  }\n\n  // True separated-feet walk: feet are drawn first, underneath the fixed body.\n  // This removes the ugly cropped-foot chunks from the previous version.\n  let lx=centerX-6,ly=by+14,rx=centerX+1,ry=by+14;\n  let lLift=false,rLift=false;\n  if(moving){\n    if(dir==='left'||dir==='right'){\n      const sign=dir==='left'?-1:1;\n      const lStride=[-2,-1,2,1][step]*sign;\n      const rStride=[2,1,-2,-1][step]*sign;\n      lx+=lStride;rx+=rStride;\n      lLift=step===0||step===1;\n      rLift=step===2||step===3;\n      if(lLift)ly-=1;if(rLift)ry-=1;\n    }else{\n      const lStride=[-1,0,1,0][step];\n      const rStride=[1,0,-1,0][step];\n      lx+=lStride;rx+=rStride;\n      lLift=step===0;\n      rLift=step===2;\n      if(lLift)ly-=2;if(rLift)ry-=2;\n      if(step===1){ly+=1;}\n      if(step===3){ry+=1;}\n    }\n  }\n  drawBaseDuckFootV2(ctx,lx,ly,lLift,flipX);\n  drawBaseDuckFootV2(ctx,rx,ry,rLift,flipX);\n\n  // Body crop ends before the feet/shadow in the approved source art.\n  if(flipX){\n    ctx.save();ctx.translate(bodyX+bodyW,0);ctx.scale(-1,1);\n    ctx.drawImage(img,0,0,112,90,0,bodyY,bodyW,bodyH);ctx.restore();\n  }else{\n    ctx.drawImage(img,0,0,112,90,bodyX,bodyY,bodyW,bodyH);\n  }\n\n  if(shooting&&!dead)drawDuckAngryFaceV2(ctx,x,y+bodyBob,dir);\n\n  if(dashing){\n    ctx.save();ctx.globalAlpha=.13;ctx.fillStyle='#fff1cf';\n    ctx.beginPath();ctx.ellipse(centerX,by+18,13,4,0,0,Math.PI*2);ctx.fill();ctx.restore();\n  }\n  ctx.restore();\n  return true;\n}\n`;

  src=before+replacement+after;
  writeFileSync(file,src,'utf8');
}
