import{existsSync,readFileSync,readdirSync,writeFileSync}from'node:fs';import path from'node:path';
function renameCurrencyText(s){return s.replaceAll('MONEDAS DORADAS','MONEDAS').replaceAll('Monedas doradas','Monedas').replaceAll('monedas doradas','monedas').replaceAll('DORADAS GUARDADAS','MONEDAS GUARDADAS').replaceAll('DORADAS','MONEDAS').replaceAll('DORADA · BOLSA DE DINERO','MONEDA · BOLSA DE DINERO').replaceAll('GOLDEN COINS','COINS').replaceAll('Golden coins','Coins').replaceAll('golden coins','coins');}
function patchText(file){if(!existsSync(file))return;const s=readFileSync(file,'utf8'),n=renameCurrencyText(s);if(n!==s)writeFileSync(file,n);}
export function applyDuckMenuCurrencyPolish(gameDir){const render=path.join(gameDir,'game','render.ts');let r=readFileSync(render,'utf8');const framed="function renderMenuUI(engine:GameEngine){renderMenuUILegacy(engine);dhChrome(engine,'menu' as DHMenuMode);}";if(!r.includes(framed))throw new Error('Menu currency polish failed: main menu chrome wrapper not found');r=r.replace(framed,'function renderMenuUI(engine:GameEngine){renderMenuUILegacy(engine);}');r=renameCurrencyText(r);writeFileSync(render,r);for(const rel of['game/i18n.ts','game/engine.ts','game/events.ts','game/data.ts'])patchText(path.join(gameDir,rel));const loc=path.join(gameDir,'localization');if(existsSync(loc))for(const name of readdirSync(loc))if(name.endsWith('.ts'))patchText(path.join(loc,name));}
