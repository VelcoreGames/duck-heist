// Duck Heist audio v2 — procedural sound design + lightweight adaptive music.
// No external audio files: every sound is generated with Web Audio so the build stays portable.
let audioCtx: AudioContext | null = null;
let masterBus: GainNode | null = null;
let musicBus: GainNode | null = null;
let sfxBus: GainNode | null = null;
let limiter: DynamicsCompressorNode | null = null;
let masterVol = 0.9;
let musicVol = 0.45;
let sfxVol = 0.9;
let musicTimer: number | null = null;
let musicStep = 0;
let musicMood: 'menu' | 'run' | 'boss' | 'death' | 'off' = 'off';
let musicFloor = 0;
let testMode = false;
let priorityUntil = 0;
let noiseBuffer: AudioBuffer | null = null;
const lastSound: Record<string, number> = {};
export function setAudioTestMode(value: boolean) { testMode = value; }
function clamp(v: number) { return Math.max(0, Math.min(1, v)); }
function nowMs() { return typeof performance !== 'undefined' ? performance.now() : Date.now(); }
function allow(id: string, delay: number) { const now = nowMs(); if (now - (lastSound[id] ?? -1e6) < delay) return false; lastSound[id] = now; return true; }
export function setVolumes(master: number, music: number, sfx: number) { masterVol = clamp(master); musicVol = clamp(music); sfxVol = clamp(sfx); if (masterBus) masterBus.gain.value = masterVol; if (musicBus) musicBus.gain.value = musicVol; if (sfxBus) sfxBus.gain.value = sfxVol; }
function enabled() { return !testMode && masterVol > 0.001 && (sfxVol > 0.001 || musicVol > 0.001); }
function setupGraph(ctx: AudioContext) { if (masterBus && musicBus && sfxBus && limiter) return; masterBus = ctx.createGain(); musicBus = ctx.createGain(); sfxBus = ctx.createGain(); limiter = ctx.createDynamicsCompressor(); masterBus.gain.value = masterVol; musicBus.gain.value = musicVol; sfxBus.gain.value = sfxVol; limiter.threshold.value = -7; limiter.knee.value = 5; limiter.ratio.value = 8; limiter.attack.value = 0.002; limiter.release.value = 0.12; musicBus.connect(masterBus); sfxBus.connect(masterBus); masterBus.connect(limiter); limiter.connect(ctx.destination); }
function getCtx(): AudioContext { if (!enabled()) throw new Error('audio off'); if (!audioCtx) audioCtx = new AudioContext(); setupGraph(audioCtx); if (audioCtx.state === 'suspended') void audioCtx.resume(); return audioCtx; }
export function initAudio() { if (testMode) return; try { const ctx = getCtx(); if (ctx.state === 'suspended') void ctx.resume(); } catch { /* audio may require gesture */ } }
function destination(kind: 'sfx' | 'music') { const ctx = getCtx(); setupGraph(ctx); return kind === 'sfx' ? sfxBus! : musicBus!; }
function oscTone(type: OscillatorType, from: number, to: number, dur: number, vol: number, delay = 0, kind: 'sfx' | 'music' = 'sfx', detune = 0) { if (!enabled() || (kind === 'sfx' ? sfxVol : musicVol) <= .001 || dur <= 0) return; try { const ctx = getCtx(), t0 = ctx.currentTime + delay; const osc = ctx.createOscillator(), g = ctx.createGain(); osc.type = type; osc.detune.value = detune; osc.frequency.setValueAtTime(Math.max(1, from), t0); osc.frequency.exponentialRampToValueAtTime(Math.max(1, to), t0 + dur); g.gain.setValueAtTime(0.0001, t0); g.gain.linearRampToValueAtTime(vol, t0 + Math.min(.012, dur * .2)); g.gain.exponentialRampToValueAtTime(.0001, t0 + dur); osc.connect(g); g.connect(destination(kind)); osc.start(t0); osc.stop(t0 + dur + .02); osc.onended = () => { osc.disconnect(); g.disconnect(); }; } catch { /* ignore */ } }
function filteredNoise(dur: number, vol: number, delay = 0, low = 90, high = 12000, q = .7, decay = 0.22, kind: 'sfx' | 'music' = 'sfx') { if (!enabled() || (kind === 'sfx' ? sfxVol : musicVol) <= .001 || dur <= 0) return; try { const ctx = getCtx(), t0 = ctx.currentTime + delay; if (!noiseBuffer || noiseBuffer.sampleRate !== ctx.sampleRate) { noiseBuffer = ctx.createBuffer(1, Math.floor(ctx.sampleRate * 2), ctx.sampleRate); const d = noiseBuffer.getChannelData(0); for (let i = 0; i < d.length; i++) d[i] = Math.random() * 2 - 1; } const src = ctx.createBufferSource(), hp = ctx.createBiquadFilter(), lp = ctx.createBiquadFilter(), g = ctx.createGain(); src.buffer = noiseBuffer; hp.type = 'highpass'; hp.frequency.value = low; hp.Q.value = q; lp.type = 'lowpass'; lp.frequency.value = high; lp.Q.value = q; g.gain.setValueAtTime(vol, t0); g.gain.exponentialRampToValueAtTime(.0001, t0 + Math.max(.01, dur * (1 + decay))); src.connect(hp); hp.connect(lp); lp.connect(g); g.connect(destination(kind)); src.start(t0, Math.random()); src.stop(t0 + dur + .03); src.onended = () => { src.disconnect(); hp.disconnect(); lp.disconnect(); g.disconnect(); }; } catch { /* ignore */ } }
function metallic(freq: number, dur: number, vol: number, delay = 0, kind: 'sfx' | 'music' = 'sfx') { oscTone('sine', freq, freq * .99, dur, vol, delay, kind); oscTone('sine', freq * 2.13, freq * 2.08, dur * .62, vol * .42, delay, kind, 5); oscTone('triangle', freq * 3.31, freq * 3.2, dur * .38, vol * .20, delay, kind, -8); }
function thump(freq = 95, dur = .12, vol = .05, delay = 0, kind: 'sfx' | 'music' = 'sfx') { oscTone('sine', freq * 1.8, freq, dur, vol, delay, kind); filteredNoise(Math.min(.055, dur), vol * .45, delay, 80, 1800, .8, .1, kind); }
function click(freq = 750, vol = .025, delay = 0, kind: 'sfx' | 'music' = 'sfx') { filteredNoise(.022, vol * .8, delay, 500, 8000, .8, .08, kind); oscTone('triangle', freq, freq * .72, .032, vol, delay, kind); }
const MENU_BASS = [110, 0, 123.47, 0, 146.83, 0, 123.47, 0];
const RUN_BASS = [73.42, 0, 87.31, 0, 98, 87.31, 73.42, 0];
const BOSS_BASS = [65.41, 77.78, 65.41, 55, 65.41, 87.31, 77.78, 55];
const DEATH_BASS = [146.83, 130.81, 123.47, 110, 98, 0, 87.31, 0];
const FLOOR_TRANSPOSE = [1, 1.122, .89, 1.189, .75, .667];
export function setMusic(mood: 'menu' | 'run' | 'boss' | 'death' | 'off', floor = musicFloor) { if (testMode) return; floor = Math.max(0, Math.min(5, floor)); if (musicMood === mood && musicFloor === floor) return; musicFloor = floor; musicMood = mood; if (musicTimer !== null) { clearInterval(musicTimer); musicTimer = null; } if (mood === 'off') return; musicStep = 0; const bpm = mood === 'death' ? 58 : mood === 'boss' ? 138 : mood === 'run' ? 112 : 86; const beat = 60000 / bpm / 2; tickMusic(mood, beat); musicTimer = window.setInterval(() => tickMusic(mood, beat), beat); }
function tickMusic(mood: 'menu' | 'run' | 'boss' | 'death', beat: number) { if (musicVol <= .001 || masterVol <= .001) return; const seq = mood === 'death' ? DEATH_BASS : mood === 'menu' ? MENU_BASS : mood === 'run' ? RUN_BASS : BOSS_BASS; const s = musicStep % seq.length, trans = FLOOR_TRANSPOSE[musicFloor] ?? 1, f = seq[s] * trans; const sec = beat / 1000; if (f > 0) { if (mood === 'death') { oscTone('triangle', f, f * .985, sec * 1.75, .028, 0, 'music'); oscTone('sine', f / 2, f * .495, sec * 1.9, .018, 0, 'music'); if (s % 4 === 0) metallic(f * 2, .35, .006, .08, 'music'); } else { oscTone(mood === 'boss' ? 'sawtooth' : 'triangle', f * 2, f, sec * .82, mood === 'menu' ? .025 : .03, 0, 'music'); if (mood !== 'menu') oscTone('sine', f * 4, f * 3.96, sec * .28, .012, .018, 'music'); } } if (mood === 'death') { if (s === 0 || s === 4) thump(48, .22, .014, 0, 'music'); } else if (mood !== 'menu' && s % 2 === 1) filteredNoise(.028, mood === 'boss' ? .020 : .012, 0, 3000, 11000, .6, .06, 'music'); if (mood === 'menu' && (s === 1 || s === 5)) click(980 + s * 35, .009, 0, 'music'); if (mood === 'menu' && s === 6) metallic(659.25, .18, .010, 0, 'music'); if (mood === 'boss' && s % 2 === 0) thump(62, .13, .022, 0, 'music'); musicStep++; }
export function playDeathMusic() { setMusic('death', musicFloor); }
;(globalThis as any).__duckDeathMusic = playDeathMusic;
export function stopMusic() { setMusic('off'); }
export function playQuack(variant?: number) { if (!enabled() || sfxVol <= .001 || !allow('quack', 70)) return; try { const ctx = getCtx(), v = variant !== undefined ? Math.abs(variant) % 4 : Math.floor(Math.random() * 4), t0 = ctx.currentTime; const cfg = [{ p: 195, e: 118, d: .245, f1: 720, f2: 1660 },{ p: 215, e: 132, d: .215, f1: 790, f2: 1810 },{ p: 178, e: 106, d: .270, f1: 680, f2: 1540 },{ p: 235, e: 145, d: .190, f1: 840, f2: 1920 }][v]; filteredNoise(.045, .032, 0, 350, 5200, 1.2, .08); const src = ctx.createOscillator(), src2 = ctx.createOscillator(), f1 = ctx.createBiquadFilter(), f2 = ctx.createBiquadFilter(), g1 = ctx.createGain(), g2 = ctx.createGain(), mix = ctx.createGain(); src.type = 'sawtooth'; src2.type = 'square'; src.frequency.setValueAtTime(cfg.p, t0); src.frequency.exponentialRampToValueAtTime(cfg.p * 1.18, t0 + cfg.d * .22); src.frequency.exponentialRampToValueAtTime(cfg.e, t0 + cfg.d); src2.frequency.setValueAtTime(cfg.p * 1.005, t0); src2.frequency.exponentialRampToValueAtTime(cfg.e * .995, t0 + cfg.d); f1.type = 'bandpass'; f1.frequency.value = cfg.f1; f1.Q.value = 5.2; f2.type = 'bandpass'; f2.frequency.value = cfg.f2; f2.Q.value = 7.0; g1.gain.value = .9; g2.gain.value = .34; mix.gain.setValueAtTime(.0001, t0); mix.gain.linearRampToValueAtTime(.13, t0 + .018); mix.gain.exponentialRampToValueAtTime(.065, t0 + cfg.d * .52); mix.gain.exponentialRampToValueAtTime(.0001, t0 + cfg.d); src.connect(f1); f1.connect(g1); g1.connect(mix); src2.connect(f2); f2.connect(g2); g2.connect(mix); mix.connect(destination('sfx')); src.start(t0); src2.start(t0); src.stop(t0 + cfg.d + .02); src2.stop(t0 + cfg.d + .02); src2.onended = () => { src.disconnect(); src2.disconnect(); f1.disconnect(); f2.disconnect(); g1.disconnect(); g2.disconnect(); mix.disconnect(); }; } catch { /* ignore */ } }
export function playQuackReady() { if (!enabled() || !allow('quack-ready', 180)) return; metallic(880, .13, .025); oscTone('sine', 1047, 1568, .12, .026, .03); click(1450, .012, .06); }
export function playDash() { if (!enabled() || !allow('dash', 70)) return; filteredNoise(.17, .060, 0, 320, 8500, .7, .11); filteredNoise(.11, .035, .015, 1800, 12500, .5, .08); oscTone('triangle', 210, 760, .115, .035); oscTone('sine', 540, 260, .085, .024, .025); }
export function playDashReady() { if (!enabled() || !allow('dash-ready', 160)) return; oscTone('sine', 410, 820, .07, .024); oscTone('triangle', 820, 1240, .055, .021, .04); click(1200, .010, .065); }
export function playShoot(weapon = 'quack_blaster') { if (!allow(`shot-${weapon}`, weapon === 'quack_laser' ? 85 : weapon === 'feather_gun' ? 24 : 35)) return; switch (weapon) { case 'breadcrumb_shotgun': filteredNoise(.145, .070, 0, 110, 4800, .8, .10); thump(72, .15, .062); oscTone('triangle', 160, 52, .13, .035); break; case 'baguette_launcher': thump(67, .18, .070); filteredNoise(.12, .040, 0, 250, 3300, .6, .11); oscTone('sine', 125, 42, .19, .035); break; case 'feather_gun': filteredNoise(.034, .020, 0, 1800, 11500, .5, .06); oscTone('triangle', 980, 420, .034, .020); break; case 'quack_laser': oscTone('sawtooth', 210, 230, .105, .020); oscTone('sine', 620, 650, .11, .028); oscTone('sine', 1240, 1190, .07, .010); break; case 'golden_egg_revolver': filteredNoise(.09, .045, 0, 600, 8500, .8, .07); thump(105, .10, .038); metallic(1250, .08, .020, .015); break; case 'bread_boomerang': filteredNoise(.085, .030, 0, 900, 9000, .6, .08); oscTone('triangle', 310, 560, .10, .025); break; case 'rubber_duck_cannon': thump(88, .11, .035); oscTone('sine', 610, 330, .085, .030); oscTone('triangle', 980, 540, .055, .014, .015); break; case 'tactical_toaster': click(520, .025); oscTone('square', 250, 105, .075, .028); filteredNoise(.045, .018, .015, 1200, 7000, .6, .08); break; case 'egg_cannon': thump(92, .12, .040); click(1500, .015, .045); oscTone('triangle', 330, 110, .105, .025); break; case 'baguette_sniper': filteredNoise(.105, .060, 0, 450, 12000, .9, .06); thump(58, .21, .074); metallic(1700, .09, .018, .022); break; case 'plasma_baker': oscTone('sawtooth', 150, 620, .13, .034); oscTone('sine', 330, 980, .12, .030, .012); filteredNoise(.065, .020, 0, 2200, 9500, .6, .08); break; case 'homing_crumbs': oscTone('sine', 760, 320, .055, .021); click(980, .010, .012); break; default: filteredNoise(.045, .025, 0, 700, 7600, .7, .05); oscTone('square', 760, 250, .070, .028); oscTone('triangle', 360, 220, .055, .012, .018); break; } }
export function playHit() { if (allow('hit', 42)) { click(410, .019); oscTone('triangle', 350, 105, .045, .018); } }
export function playPickup() { metallic(740, .12, .026); oscTone('sine', 740, 1320, .13, .028, .03); }
export function playExplosion() { if (!allow('explosion', 55)) return; filteredNoise(.29, .078, 0, 35, 5200, .7, .18); thump(48, .28, .075); oscTone('sine', 120, 34, .26, .032, .015); }
export function playHurt() { priorityUntil = nowMs() + 420; filteredNoise(.10, .040, 0, 300, 5200, .8, .08); oscTone('square', 235, 58, .17, .055); }
export function playEquip() { click(920, .020); metallic(840, .10, .026, .018); oscTone('triangle', 620, 1180, .09, .022, .04); }
export function playWeaponSwap() { click(390, .024); oscTone('square', 430, 820, .055, .028); filteredNoise(.045, .015, .015, 800, 6500, .6, .08); }
export function playStairs() { [293.66, 392, 493.88, 659.25].forEach((n, i) => { metallic(n, .16, .024, i * .085); oscTone('sine', n / 2, n, .13, .014, i * .085); }); }
export function playBossRoar() { priorityUntil = nowMs() + 650; filteredNoise(.42, .060, 0, 80, 3000, 1.1, .15); oscTone('sawtooth', 150, 48, .46, .055); oscTone('triangle', 92, 56, .38, .038, .03); }
export function playDoorLock() { if (!allow('door-lock', 100)) return; metallic(190, .10, .035); click(280, .022, .055); thump(68, .15, .035, .06); metallic(128, .13, .025, .105); }
export function playDoorUnlock() { if (!allow('door-unlock', 100)) return; click(360, .024); metallic(330, .09, .024, .025); metallic(520, .11, .023, .09); metallic(780, .14, .021, .16); }
export function playUiMove() { if (allow('ui-move', 42)) { click(620, .018); oscTone('triangle', 300, 245, .028, .010); } }
export function playUiSelect() { if (!allow('ui-select', 55)) return; click(920, .026); metallic(720, .09, .022, .018); oscTone('sine', 880, 1175, .085, .018, .055); }
export function playUiBack() { if (!allow('ui-back', 55)) return; click(520, .020); oscTone('triangle', 520, 260, .085, .025); filteredNoise(.045, .010, 0, 900, 7000, .5, .08); }
export function playDeny() { if (!allow('deny', 100)) return; oscTone('square', 190, 145, .085, .032); oscTone('square', 160, 120, .085, .026, .075); }
export function playCoin() { if (!allow('coin', 36)) return; const vol = nowMs() < priorityUntil ? .010 : .024, base = 1120 + Math.random() * 210; metallic(base, .09, vol); oscTone('sine', base * 1.5, base * 1.45, .055, vol * .55, .015); }
export function playHeal() { oscTone('sine', 523.25, 659.25, .13, .028); oscTone('sine', 659.25, 1046.5, .20, .026, .09); metallic(1318.5, .12, .012, .16); }
export function playCritical() { if (allow('crit', 70)) { filteredNoise(.05, .025, 0, 1200, 11000, .5, .06); metallic(1480, .08, .022); oscTone('triangle', 940, 300, .085, .026); } }
export function playEnemyDeath() { if (allow('death', 58)) { filteredNoise(.10, .026, 0, 500, 6000, .6, .10); oscTone('triangle', 210, 62, .105, .025); click(330, .012, .025); } }
export function playRoomClear() { if (!allow('room-clear', 240)) return; click(420, .023); [392, 493.88, 587.33, 783.99].forEach((n, i) => metallic(n, .17, .024, .045 + i * .060)); oscTone('triangle', 220, 330, .18, .018, .19); oscTone('sawtooth', 225, 135, .105, .010, .27); filteredNoise(.025, .008, .27, 500, 3500, .8, .05); }
export function playBossWin() { priorityUntil = nowMs() + 850; filteredNoise(.42, .050, 0, 45, 4800, .8, .18); thump(52, .30, .060); [261.63, 329.63, 392, 523.25, 659.25].forEach((n, i) => metallic(n, .28, .028, .09 + i * .105)); metallic(1046.5, .22, .022, .58); }
export function playRarityPickup(rarity: number) { playPickup(); if (rarity >= 2) metallic(1046.5, .15, .018, .09); if (rarity >= 4) [783.99, 1046.5, 1318.5].forEach((n, i) => metallic(n, .30, .022, .15 + i * .075)); }
export function playReturn() { if (allow('return', 115)) { filteredNoise(.06, .015, 0, 1200, 8500, .5, .07); oscTone('triangle', 430, 840, .085, .018); } }
export function playBounce() { if (allow('bounce', 65)) { oscTone('sine', 760, 410, .065, .022); click(690, .010); } }
export function playFootstep() { if (allow('feet', 145)) { filteredNoise(.026, .009, 0, 140, 1500, .5, .08); thump(82 + Math.random() * 14, .030, .008); } }
export function playDoorStyle(style: string) { if (style === 'boss') playDoorLock(); else if (style === 'gold' || style === 'purple') { metallic(660, .15, .021); metallic(990, .14, .017, .07); } else if (style === 'green') { click(740, .015); metallic(980, .10, .016, .05); } else { click(260, .014); filteredNoise(.055, .012, 0, 300, 3200, .6, .08); } }
export function playDanger(kind: 'aim' | 'charge' | 'camera' = 'aim') { if (!allow(`danger-${kind}`, 190)) return; priorityUntil = nowMs() + 350; if (kind === 'charge') { filteredNoise(.055, .025, 0, 900, 6500, .6, .07); oscTone('triangle', 145, 310, .16, .040); thump(78, .09, .020, .07); } else if (kind === 'camera') { oscTone('square', 510, 540, .075, .025); oscTone('square', 720, 760, .075, .022, .10); click(1300, .010, .19); } else { oscTone('sine', 1120, 1120, .07, .032); oscTone('sine', 1460, 1460, .07, .026, .09); } }
export function setSoundEnabled(v: boolean) { setVolumes(v ? .9 : 0, musicVol, sfxVol); }
